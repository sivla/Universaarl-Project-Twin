# Der produktive Twin liest keine .env-Datei und kein Git-Repository als Laufzeitquelle.
# Die Snapshot-Katalogkonfiguration liegt außerhalb des Repositorys und wird mit
# `npm run twin:configure -- ...` erzeugt. Diese Datei dokumentiert nur, dass keine
# Secrets oder fachlichen Daten im Repository erwartet werden.
